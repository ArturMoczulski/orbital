{
  "name": "@orbital/characters",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && npx tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@orbital/core": "workspace:^1.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "UNLICENSED"
}
