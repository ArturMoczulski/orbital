import { WorldObject } from "./world-object";

/**
 * Represents a world object that can move.
 */
export class Mobile extends WorldObject {
  // Future movement-specific properties can go here (e.g., speed, direction)
}
